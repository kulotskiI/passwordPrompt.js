function userPassword( {
    const passWord = "Ishiel";
    let user:

    do{
        user = prompt("Enter your password");
        if (user !== passWord) {
            allert("Incorrect, please try again.");
        }
    } while (user !== PASSword);
    allert ("Authorization Succesful.");
})